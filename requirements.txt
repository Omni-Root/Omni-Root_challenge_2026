# Dependências multiplataforma (Windows, Windows Embedded, Linux, macOS)
ultralytics
opencv-python-headless
numpy

# Só instala/é necessária rodando de verdade num Raspberry Pi com GPIO
# físico (sensor ultrassônico). Em qualquer outro sistema (Windows,
# notebook, etc.) o "sys_platform == linux" abaixo já impede o pip de
# tentar instalar — e o main.py já cai sozinho para sensor simulado se
# ela não estiver presente.
RPi.GPIO; sys_platform == "linux"
